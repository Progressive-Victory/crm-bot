// class StateManager extends CachedManager<StateAbbreviation, State, StateAbbreviation> {
// 	constructor(client: Client, iterable?: Iterable<State>) {
// 		super(client, State, iterable);

// 		this.guild = gu;
// ild
// 	}

// 	_add(data: State, cache: boolean = true) {
// 		return super._add(data, cache, { id: data.abbreviation, extras: [this.guild] });
// 	}
// }
